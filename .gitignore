# Virtual environments
venv/
.venv/

# Generated artifacts (see strc_paths / pipeline)
output/
reports/

# Legacy plot dir at repo root
plots/

# Large / binary simulation outputs (any location)
*.npy
*.npz

# Python
__pycache__/
*.py[cod]
*$py.class
.Python
*.so
.pytest_cache/
.mypy_cache/

# Environment and OS
.env
.env.*
.DS_Store

# IDE / local tooling
.cursor/

# Next.js
web/node_modules/
web/.next/
web/out/
web/*.tsbuildinfo

# Optional: root-level fetched / run artifacts (regenerable; avoid accidental commit)
*_data.json
*_options.json
yield_curve.json
# Website chart JSON — written every market snapshot from the live curve
# (or yield_curve_fallback.json). Must be tracked so Vercel can compile it.
!web/public/data/yield_curve.json
treasury_extracted_data.json
btc_historical_data.json
sata_valuation_results.json
sata_valuation_report.html
